# PLP Africa — Week 3: React + Tailwind App

## Overview
Responsive React application built with Vite and Tailwind CSS. Includes TaskManager with localStorage persistence, API integration (JSONPlaceholder), routing, theme switcher, and reusable components.

## Run locally
```bash
npm install
npm run dev
# open http://localhost:5173
